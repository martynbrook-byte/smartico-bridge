# Dependencies
node_modules/

# Environment secrets
.env

# Uploaded CSV files (transient)
uploads/

# Persisted data — all of this lives on the Railway volume, not in git.
# Keep folder structure via .gitkeep files, but never commit live data.
data/settings.json
data/datasets/*.json
data/pipelines/*.json
data/dropzones/*.json

# macOS
.DS_Store

# Editor
.vscode/
*.swp
