# HTTP port (Railway sets this automatically — don't override on Railway)
PORT=3001

# Directory where datasets and settings are persisted.
# On Railway, point this at your mounted volume path (e.g. /data).
DATA_DIR=./data
